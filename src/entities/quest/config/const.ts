import { ElementSize } from '@shared/model';

export const DEFAULT_PREVIEW_SIZE: ElementSize = {
  width: 344,
  height: 232,
};

export const DEFAULT_TAG_SIZE: ElementSize = {
  width: 11,
  height: 14,
};

export enum TagIconId {
  Person = '#icon-person',
  Level = '#icon-level',
}
